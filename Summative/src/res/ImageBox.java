package res;

import java.awt.image.BufferedImage;

public class ImageBox extends Clickable{

	public ImageBox(int x, int y, BufferedImage image, String name) {
		super(x, y, image, name);
	}
}
